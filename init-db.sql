-- Database initialization for Payload CMS
-- Payload with push: true handles all schema automatically
-- This file only adds extensions

-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Done! Payload handles the rest with push: true
